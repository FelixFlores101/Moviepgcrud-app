<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
    <h1>Movies I Should Be Watching</h1>
    <ul>
            <% data.map((movie, index) => { %>
                <li>
                    <div>
                        <a href="/movies/<%= movie.id %>">Title: <%= movie.moviename%></a> 
                        Platform: <%= movie.platform%>       
                    </div>
                    <form action="<%=`/movies/${movie.id}?_method=DELETE` %>" method="POST">
                        <input type="submit" value="Delete" />
                    </form>
                    <form action="<%=`/movies/${movie.id}/edit`%>" method="get">
                        <input type="submit" value="Update" />
                    </form>
                </li>
               
                <% }) %>


                
    </ul>
    <a href="/movies/add"><h1>Add Movie</h1></a>
    <a href="/movies">
        <h1>
                Edit Movie List
        </h1></a>
</body>
</html>